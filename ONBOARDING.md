# Onboarding Guide

## 1. このプロジェクトについて
このリポジトリは、Git の基本操作を学ぶための練習用プロジェクトです。

特徴は次のとおりです。
- 依存関係やビルド設定はありません。
- 主要な対象はテキストファイルです。
- Git の基本フローを体験することを目的としています。

## 2. リポジトリ構成
現在の構成は次のとおりです。
- [hello.txt](hello.txt): 変更やコミットの練習用ファイル
- .git: Git の管理情報

## 3. まず最初に確認すること
新規参画時に最初に確認するとよい項目です。

1. リポジトリの場所を確認する
2. 現在のブランチと状態を確認する
3. 既存ファイルの内容を把握する
4. Git の基本コマンドを試す

## 4. 基本的な作業フロー
このプロジェクトでは、次の流れで作業することが一般的です。

1. ブランチを作成する
2. ファイルを編集する
3. 変更内容を確認する
4. 変更をステージングする
5. コミットする
6. リモートにプッシュする

### 代表的なコマンド
```bash
git status
git branch
git checkout -b <branch-name>
git add .
git commit -m "説明"
git push origin <branch-name>
```

## 5. このプロジェクトでよく使う操作
- 変更内容の確認: `git status`
- 履歴の確認: `git log`
- 最新状態の取得: `git pull`
- リモートの最新情報確認: `git fetch`
- 変更の取り込み: `git merge` または `git pull`

## 6. 作業時の注意点
- 変更前には必ず `git status` を確認する
- コミット前に内容を見直す
- 共有前に `git pull` で最新状態を取り込む
- 競合が起きた場合は、内容を確認して解決する

## 7. 役に立つ覚え方
- `clone`: 初めてリポジトリをコピーする
- `fetch`: リモートの最新情報を確認して取る
- `pull`: その最新情報を自分の作業に反映する

## 8. 参考となる質問
新しいメンバーが迷いやすいポイントは次のとおりです。
- どのブランチで作業すべきか
- どのタイミングでコミットするか
- 変更を取り込むときに競合が起きたらどうするか

## 9. まとめ
このプロジェクトは、Git の基本操作を体験するための最小構成の練習用リポジトリです。
まずは、ファイルを開いて変更し、コミットして、プッシュする流れを一通り試すことで理解が深まります。
